<h3>Update Your Story</h3>
<div class="row">
{{!-- {{> _edit-category}} --}}
    <form action="/stories/{{story.id}}?_method=PUT" method="post" class="col s12">
       <input type="hidden" name="_method" value="PUT">
        <div class="row">
            <div class="input-field">
                <input type="text" name="title" value="{{story.title}}">
                <label for="title">Title</label>
            </div>
        </div>
        <div class="row">
            <div class="input-field">
                <input type="text" name="bodyImage" value="{{story.bodyImage}}">
                <label for="bodyImage">Header Image Url</label>
            </div>
        </div>
        <div class="row">
            <div class="input-field">
                <select name="status">
                    {{#select story.status}}
                    <option value="public" selected>Public</option>
                    <option value="private">Private</option>
                    <option value="upublished">Unpublished</option>
                    {{/select}}
                </select>
                <label for="status">Status</label>
            </div>
        </div>
        <div class="row">
            <div class="input-field">
                <select name="categoryType">
                    {{#select story.status}}
                    <option value="general" selected>General</option>
                    <option value="sci-fi">Sci-Fi</option>
                    <option value="travel">Travel</option>
                    <option value="personal">Biography</option>
                    {{/select}}
                </select>
                <label for="status">Category</label>
            </div>
        </div>
        <div class="row">
            <div class="switch">
                <label>
                    Off
                    <input type="checkbox" name="allowComments" class="filled-in" id="filled-in-box" {{#if story.allowComments}} checked="checked"
                        {{/if}}>
                    <span class="lever"></span>
                    On
                </label>
            </div>
            {{!-- -- <p>
                <input type="checkbox" name="allowComments" class="filled-in" id="filled-in-box" {{#if story.allowComments}} checked="checked" {{/if}}>
                <label for="filled-in-box">Allow Comments</label>
            </p> -- --}}
        </div>
        <div class="row">
            <div class="input-field">
                <h5>Story Board</h5>
                <textarea name="body">{{story.body}}</textarea>

            </div>
        </div>
        <input type="submit" value="Save" class="btn btn-large btn-circle shake-top">
    </form>
</div>